{
  "type": "module",
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "npx tsc --watch",
    "server": "live-server --open=./html/canvas.html",
    "start": "start npm run build && start npm run server"
  }
}
