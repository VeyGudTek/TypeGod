import type { User } from "@Models/User.type";

export const defaultUserData:User = {
    "main": {
        level: 1,
        health: 100,
        damage: 10,
        mana: 200,
        experience: 0,
        levelUps: 0
    }
}